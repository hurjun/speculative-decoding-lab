# Runtime dependencies (pinned). Tested on Python 3.12 and 3.13.
# Install into a project-local virtual environment:
#   python -m venv .venv && source .venv/bin/activate
#   pip install -r requirements.txt
torch==2.6.0
transformers==4.49.0
numpy==2.2.4
